<h1>All Tags</h1>

<ul class="tag">
  <% @tag.each do |tag| %>
    <li>
      <%= link_to tag.name, tag_path(tag) %>
    </li>
  <% end %>
</ul>
<br />
<%= link_to "Back to Articles", articles_path%>
